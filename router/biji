vue - router
    标签（组件）
        router-link:用于跳转链接
        router-view:用于显示 路由中内容的标签
    使用
        var router = new VueRouter({})
                参数(object)
                    router:[路由配置的对象，路由配置的对象]数组
                        路由配置的对象：Object
                            path:路径
                            component:组件

        在 new Vue({router:router})
