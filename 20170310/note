Vue_2
    :class
           1.可以通过变量的方式    设置  元素的样式
           :class="type"
           2.可以通过数组的方式  设置元素的多个类选择器
           :class="[type,uaciType]"
           3.可以通过 一个  bool 值  来设置  类选择器（不知变量）是否被选择上
           :class="{class1:isChoose}"
    methods
    watch
        数据名
            handler:函数
                新值   旧值
            deep:深复制   浅复制
    components
        组件名：组建
        组建
            props:对应的值 是一个数组
            template:HTML的末班内容
            data
    路由